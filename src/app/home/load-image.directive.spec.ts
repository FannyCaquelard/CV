import { LoadImageDirective } from './load-image.directive';

describe('LoadImageDirective', () => {
  it('should create an instance', () => {
    const directive = new LoadImageDirective(null, null, null);
    expect(directive).toBeTruthy();
  });
});
